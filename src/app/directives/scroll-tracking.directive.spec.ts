import { ScrollTrackingDirective } from './scroll-tracking.directive';

describe('ScrollTrackingDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTrackingDirective();
    expect(directive).toBeTruthy();
  });
});
